FASTAPI_URL=http://localhost:8000

# Ground-truth (MongoDB) comparison UI — dev/internal only, requires a
# MongoDB-backed labeling app running separately. Leave unset/false for
# client deployments (no MongoDB available there).
NEXT_PUBLIC_ENABLE_GT=false
